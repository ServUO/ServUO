using System;

namespace Server.Engines.ClassSystem
{
    /// <summary>
    /// Identifies each playable class by a unique enum value.
    /// Add a new entry here whenever a new class is introduced.
    /// </summary>
    public enum ClassType
    {
        None = 0,
        Avenger = 1,
        // Ninja     = 2,
        // Archmage  = 3,
    }
}
